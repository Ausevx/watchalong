/** @type {import('next').NextConfig} */
const nextConfig = {
  // Use a custom server so Socket.IO can attach — disable Next.js standalone server
  // We run via `node server/index.js`
};

export default nextConfig;
